import { JsonRenderingDirective } from './json-rendering.directive';

describe('JsonRenderingDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonRenderingDirective();
    expect(directive).toBeTruthy();
  });
});
